###
# Detect certaon objects in a camera feed
# Thos will be usefull for object tracking or data anaylsis
# For example i could set a time zone for the camera to identify and
# watch certain objects when there moved it can raise an alert
# or this can identify cars pulling into the street and automatically capture the reg
#
from imageai.Detection import ObjectDetection
import os